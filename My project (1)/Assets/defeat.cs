using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//wat heb ik nodig
//een healthbar voor de player
//je moet hp verliezen als er een enemy bij het einde komt
//en naar volgend level gaan bij defeaten van alle enemies in een lvl
public class defeat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
         
    }
}
